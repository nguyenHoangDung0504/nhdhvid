function checkName(n1,n2){if(""===n1||""===n2)return null;let same=0,maxLength=Math.max(n1.length,n2.length);for(let i=0;i<maxLength;i++)n1[i]===n2[i]&&same++;return same/maxLength>.6?n1.substring(0,same):null}