class CrawlerIH{static addedHen=null;static results=[];static async l(...names){const urls=names.map((name=>name.includes("http")?name:`https://ihentai.icu/${name}/`));CrawlerIH.addedHen||(CrawlerIH.addedHen=aw);for(const url of urls)CrawlerIH.leakData(url)}static async leakData(url){try{const response=await fetch(url);if(200===response.status&&response.ok){const data=await response.text();CrawlerIH.results.push(CrawlerIH.parseFunction(data))}}catch(error){console.error(error)}}static parseFunction(data){const doc=(new DOMParser).parseFromString(data,"text/html"),leakedData={name:"",src:"",thumbnail:"",studio:"",series:"",tags:"",desc:"",toString(){const{name:name,src:src,thumbnail:thumbnail,studio:studio,series:series,tags:tags,desc:desc}=leakedData;return`addData(new H('${name}', '${src}', '${thumbnail}', '${studio}', '${series}', '${tags}', '${desc}'))`}},ctn=doc.querySelector('div[class="tw-col-span-3 lg:tw-col-span-2"]'),iframe=ctn.querySelector("iframe"),inforBox=Array.from(ctn.querySelectorAll(".tw-mb-5")),inforSheet=ctn.querySelector(".v-sheet"),[studioBox]=inforBox.filter((node=>node.querySelector(".tw-text-sm").textContent.toLowerCase().includes("hãng"))),[seriesBox]=inforBox.filter((node=>node.querySelector(".tw-text-sm").textContent.toLowerCase().includes("trọn bộ")));function standarlize(s){return s.trim().toLowerCase()}return leakedData.name=standarlize(iframe.title),leakedData.src=iframe.src,leakedData.thumbnail=ctn.querySelector(".tw-grid").querySelector("img").src,leakedData.studio=Array.from(studioBox.querySelectorAll("a")).map((a=>a.textContent.trim())).join(","),leakedData.series=Array.from(seriesBox.querySelectorAll("a")).map((a=>a.textContent.trim())).join(","),leakedData.tags=Array.from(inforSheet.querySelectorAll(".v-chip-group a")).map((a=>standarlize(a.textContent))).join(","),leakedData.desc=inforSheet.querySelector(".tw-text-sm").textContent.trim().replaceAll("\n"," "),leakedData.toString()}static getResult(){}}